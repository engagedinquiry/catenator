$schema: "http://json-schema.org/draft-07/schema#"
$id: "https://syntaxia.io/schemas/v1/catenator.persona.human-end-user.schema.json"
title: "Human End User Persona Type Schema"
description: A consumer or product participant, not a technical or business decision-maker.

allOf:
  - $ref: "../schema-templates/persona.yaml"
  - type: object
    properties:
      core_domain:
        allOf:
          - type: object
            properties:
              role_type:
                const: human_end_user
