# Descriptors

A system is specified through descriptors. A descriptor is a lens — a structured way of examining one aspect of the system. No single descriptor describes the whole. Together they describe enough of it that an agent can build from what it has.
