branding.rename:
  macro:
    systemName: "branding.rename"
    domain: "presentation layout"
    intent: "Ensure the app's name/tagline come from build-config.yaml, never hardcoded."
    mustNever:
      - "Hardcode a product name anywhere in this component's logic"
      - "Leave a prior product name anywhere in the built app"
  meso:
    personas:
      - id: user
        doing: "Sees the app's name in the header/title, consistently"
    configSource: "build-config.yaml -> branding.productName, branding.tagline"
  micro:
    - id: scan-for-prior-names
      rule: "Search for any hardcoded prior name per branding.priorNames before renaming; report every occurrence."
    - id: single-source-of-truth
      rule: "The product name is read from build-config.yaml, never duplicated elsewhere."
