# Build config — reader-app

specPath: "apps/reader-app/specs/"
outputPath: "apps/reader-app/output/"

contentSource:
  rootDir: "docs"   # relative to repo root. content.folder-browser's
                     # two roots ("content/" and "schema/") are
                     # relative to THIS value.
  mode: "pre-authored"

techStack:
  frontend: "Angular"

branding:
  productName: "Catenator Reader"
  tagline: "Browse by persona, or view the schema"
  priorNames: []
